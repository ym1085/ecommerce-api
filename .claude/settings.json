{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -qE 'rm -rf|git reset --hard|git push --force' && echo 'BLOCKED: destructive command' >&2 && exit 2 || exit 0"
          }
        ]
      }
    ]
  }
}